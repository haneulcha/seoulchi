/**
 * 앱 전체가 공유하는 폭 원칙 — 상한 1024, 가장자리 여백은 폭에 따라 16 → 24 → 32.
 *
 * 화면마다 상한을 다르게 두지 않는다. 줄 길이는 컨테이너가 아니라 각 화면의 내부 구조가
 * 지킨다 — 홈은 카드 그리드가, 상세는 2단 그리드가 한다.
 *
 * 세로 리듬(`py-*`)은 화면마다 달라서 여기 넣지 않는다. 각 화면이 이 뒤에 덧붙인다.
 */
export const PAGE = 'mx-auto max-w-5xl px-4 sm:px-6 lg:px-8'
