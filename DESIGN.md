# 색

이 문서는 **색만** 다룹니다. 타이포와 간격은 아직 결정하지 않았고, 지금은 Tailwind 기본값을 씁니다.

값은 자동 생성됩니다. 손으로 고치기 전에 아래 "고칠 때"를 읽으세요.

| | |
| --- | --- |
| 실제로 쓰이는 파일 | `src/styles/theme.css` (`src/styles/app.css`가 import) |
| 프레임워크 무관 사본 | `docs/design/palette.css` |
| Figma 변수 | `docs/design/palette.figma.json` |

## 전략

**Restrained.** 배경과 글자는 뉴트럴이고, 색은 강조에만 씁니다.
PRODUCT.md의 "정보가 항상 장식을 이깁니다"와 "역 안내판" 성격에서 나온 선택입니다.

액센트는 **주황 `#fa9216`** 입니다. 안내판·전광판의 색이고,
이 앱에 색이 하나 있다면 무엇인가에 대한 답입니다.

## 스케일

여섯 개 스케일, 각 11 stop. 이 값이 프리미티브이고 테마와 무관하게 고정입니다.

### 액센트 (주황) — `accent`

| stop | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hex | `#fff6ee` | `#ffeede` | `#ffdfc3` | `#ffc99b` | `#ffab5d` | `#fa9216` | `#d27800` | `#a96000` | `#874c03` | `#6e3f0d` | `#3e2001` |

### 뉴트럴 — `neutral`

| stop | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hex | `#fafafa` | `#f4f4f5` | `#e6e6e8` | `#d4d4d8` | `#9f9fa9` | `#71717c` | `#52525c` | `#3f3f47` | `#27272b` | `#18181c` | `#09080b` |

### 오류 (빨강) — `error`

| stop | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hex | `#fff5f5` | `#ffe6e6` | `#ffcece` | `#ffa8a8` | `#ff6f70` | `#fb2c36` | `#dd0016` | `#b7000f` | `#98010e` | `#7d1216` | `#4f0a0b` |

### 경고 (노랑) — `warning`

| stop | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hex | `#faf8ec` | `#f7f2d6` | `#f1e8b2` | `#e9d97d` | `#dbc53c` | `#ccb500` | `#a99600` | `#877700` | `#6c5f00` | `#584d00` | `#2f2900` |

### 성공 (초록) — `success`

| stop | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hex | `#eefcf2` | `#d9f9e4` | `#b5f5cb` | `#80eba7` | `#42d87a` | `#00c65a` | `#00a648` | `#00863c` | `#006b32` | `#00592b` | `#003216` |

### 정보 (파랑) — `info`

| stop | 50 | 100 | 200 | 300 | 400 | 500 | 600 | 700 | 800 | 900 | 950 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| hex | `#f3f8ff` | `#e1eeff` | `#c3deff` | `#92c6ff` | `#56a2ff` | `#3280ff` | `#2d68e5` | `#2954c1` | `#26459e` | `#223c81` | `#162452` |

**경고 노랑은 액센트와 겹치지 않도록 다시 만든 값입니다.** 이전 램프는 OKLCH 색상각이
99°에서 46°로 흘러내려 어두운 쪽이 액센트 주황(62°)보다 더 붉었습니다.
solid끼리 ΔE가 0.016으로 사실상 같은 색이었습니다. 색상각을 100°로 고정해
solid ΔE 0.098로 벌렸습니다 — 액센트와 오류의 거리(0.164)에 준하는 분리입니다.

## 역할

이름 규칙: 프리미티브는 `--color-{스케일}-{stop}`, 역할은 `--color-{스케일}-{역할}`.
Tailwind 유틸리티가 그대로 따라옵니다 — `bg-accent-solid`, `text-success-text`.

| 역할 | 변수 | 라이트 | 다크 | 쓰는 곳 |
| --- | --- | --- | --- | --- |
| 은은한 배경 | `--color-{스케일}-subtle-bg` | 50 | 950 | 칩·배너 바탕 |
| 호버 배경 | `--color-{스케일}-hover-bg` | 100 | 900 | 위의 한 단계 진한 상태 |
| 테두리 | `--color-{스케일}-border` | 200 | 800 | 구분선·외곽선 |
| 솔리드 | `--color-{스케일}-solid` | 600 | 600 | 버튼 배경 |
| 솔리드 위 글자 | `--color-{스케일}-on-solid` | 흑/백 | 좌동 | solid 위에만 |
| 텍스트 | `--color-{스케일}-text` | 800 | 300 | 링크·강조 글자 |
| 진한 텍스트 | `--color-{스케일}-text-strong` | 900 | 200 | subtle-bg 위 제목 |

`solid`과 `on-solid`는 다크에서 재선언되지 않습니다 — 테마를 가로질러 고정입니다.
나머지는 사다리의 반대쪽 자리를 씁니다(라이트 stop + 다크 stop = 1100).

스케일별 `on-solid`:

| 스케일 | solid | 글자 | 대비 |
| --- | --- | --- | --- |
| accent | `#d27800` | 검정 | 6.43 |
| neutral | `#52525c` | 흰색 | 7.72 |
| error | `#dd0016` | 흰색 | 5.14 |
| warning | `#a99600` | 검정 | 7.05 |
| success | `#00a648` | 검정 | 6.55 |
| info | `#2d68e5` | 흰색 | 4.97 |

빨강과 파랑만 흰 글자입니다. 나머지는 밝은 색이라 검정이 대비가 높습니다.

## 표면과 잉크

뉴트럴에서만 나오는 네 개. 스케일 역할과 달리 이름에 스케일이 없습니다 —
페이지에 하나씩만 있는 자리이기 때문입니다.

| 역할 | 변수 | 라이트 | 다크 | 흰 배경 대비 |
| --- | --- | --- | --- | --- |
| 페이지 배경 | `--color-surface` | `#ffffff` | `#09080b` (950) | — |
| 제목·본문 | `--color-ink` | `#18181c` (900) | `#f4f4f5` (100) | 17.70 |
| 메타 한 줄 | `--color-ink-muted` | `#52525c` (600) | `#d4d4d8` (300) | 7.72 |
| 가장 약한 보조 | `--color-ink-subtle` | `#71717c` (500) | `#9f9fa9` (400) | 4.82 |

세 단계인 이유: 화면에 실제로 세 층이 있습니다. 제목·코멘트 / 장소·기간·요금 한 줄 /
주차 라벨·갱신 시각·지하철 정보. 두 단계로 접으면 메타 한 줄이 제목만큼 진해지거나
보조만큼 흐려집니다.

## 대비

**54건 전부 WCAG AA를 통과합니다. 최저 4.82** (`ink-subtle` 흰 배경 위, 기준 4.5).

라이트·다크 양쪽에서 각 스케일의 text가 페이지 배경·subtle-bg·hover-bg 위에서,
text-strong이 subtle-bg 위에서, on-solid가 solid 위에서 4.5 이상인지 검사한 결과입니다.

PRODUCT.md가 대비를 접근성 이전에 **기능 요건**으로 못박고 있습니다 — 밖에서 햇빛 아래
보는 것이 핵심 시나리오이기 때문입니다. 그래서 미달을 감수하는 토큰은 두지 않았습니다.
이 시스템의 이전 판에는 미달이 5건 있었고, `text`를 700→800, `solid`을 500→600으로
내려 전부 해소했습니다.

경계에 가장 가까운 자리:

- `ink-subtle on surface` — 4.82
- `info-on-solid on info-solid` — 4.97
- `error-on-solid on error-solid` — 5.14
- `warning-text on warning-hover-bg` — 5.69
- `[dark] info-text on info-hover-bg` — 5.78

## 다크

루트(`<html>`)에 `.dark` 클래스가 붙으면 켜집니다.

상태는 **세 가지**입니다 — `system` / `light` / `dark`. 오른쪽 위 버튼이 이 순서로 순환합니다.
기본은 `system`이고, 그때는 OS의 `prefers-color-scheme`을 따릅니다. `light`나 `dark`로
고정해 두면 OS가 바뀌어도 따라가지 않습니다. 선택은 `localStorage['seoulchi:theme']`에 남습니다.

로직은 `src/lib/theme.ts`에 있습니다. 화면에 실제로 적용되는 건 언제나 라이트 아니면 다크이고,
`system`은 "고르지 않음"이지 네 번째 색이 아닙니다 — 그 판정이 `resolveDark`입니다.

**깜빡임(FOUC)을 막는 장치가 있습니다.** GitHub Pages가 내보내는 정적 HTML의 `<html>`에는
클래스가 없어서, 하이드레이션 뒤에 `.dark`를 붙이면 다크 사용자가 매번 흰 화면을 한 번 봅니다.
그래서 `THEME_INIT_SCRIPT`를 `<head>`에 인라인으로 넣어 첫 페인트 전에 클래스를 확정합니다.
실측으로 확인했습니다 — 스크립트가 도는 시점에 `document.body`가 아직 `null`이고
`readyState`가 `loading`입니다. 즉 어떤 것도 그려지기 전입니다.

그 대가로 이 스크립트는 `theme.ts`의 다른 함수를 쓸 수 없습니다(번들보다 먼저 돕니다).
규칙이 `resolveDark`와 갈라지면 깜빡임 대신 **틀린 테마가 뜨는** 더 나쁜 버그가 되므로,
`tests/lib/theme.test.ts`가 그 문자열을 실제로 실행해 두 답이 같은지 검사합니다.

`<html>`에 `suppressHydrationWarning`이 붙어 있는 것도 같은 이유입니다. 서버 HTML에는
클래스가 없고 클라이언트에는 있으니 React가 불일치로 보고 매번 에러를 냅니다.
이 요소의 속성에 한해 경고를 끕니다 — React는 이 불일치를 되돌리지 않으므로 동작은 문제없습니다.

## 쓰는 법

- **배경과 글자는 `surface`/`ink` 계열, 색은 강조에만.** 스케일 역할을 본문에 쓰지 마세요.
- **상태색은 브랜드 색이 아닙니다.** error·warning·success·info를 브랜드에 맞춰 바꾸지 마세요.
- **다크에서 색을 새로 만들지 않습니다.** 역할이 가리키는 stop만 바뀝니다.
- **프리미티브(`accent-500` 등)를 화면에서 직접 쓰지 마세요.** 역할을 쓰면 다크가 따라옵니다.
  프리미티브를 직접 쓰면 라이트에 고정됩니다.
- **Tailwind 기본 팔레트는 꺼져 있습니다.** `theme.css`의 `--color-*: initial` 때문에
  `text-gray-500` 같은 클래스는 아예 컴파일되지 않습니다. 시스템 밖 색이 조용히 새는 걸 막습니다.
  `white`·`black`·`transparent`·`current`만 남겨 뒀습니다.

## 고칠 때

값은 `docs/design/`의 세 파일과 이 문서에 나뉘어 있고 전부 같은 근거에서 나옵니다.
하나만 고치면 어긋납니다. 색을 바꾸려면:

1. 스케일(프리미티브)을 정합니다.
2. 역할이 가리키는 stop을 정합니다.
3. 라이트·다크 양쪽에서 대비를 다시 잽니다 — 이 문서의 "54건 전부 통과"가 근거입니다.
4. `src/styles/theme.css`, `docs/design/palette.css`, `docs/design/palette.figma.json`,
   이 문서를 함께 갱신합니다.

## 아직 안 정한 것

- **액센트가 화면에 아직 안 쓰입니다.** 주황은 정의만 돼 있고 쓰는 곳이 없습니다.
  버튼·링크·현재 선택·"오늘 끝남" 같은 자리 중 어디에 줄지는 다음 결정입니다.
- **`src/lib/colors.ts`의 카테고리 폴백 8색은 이 시스템 밖에 있습니다.**
  Tailwind 600 톤 하드코딩이고, 이미지가 안 뜰 때만 보입니다.
  Restrained 전략과 어긋나지만 기능(카테고리 구분)이 있어 그대로 뒀습니다.
- **요금 초록이 유료에도 붙습니다.** `text-success-text`가 `10,000원`에도 적용됩니다.
  초록은 "무료"에만 두는 게 맞습니다(UI/UX 진단 지적).
- **타이포와 간격.** 시스템 없이 Tailwind 기본값입니다.
